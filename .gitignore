out/
__pycache__/
*.pyc
assets/bgm/
.venv/
archive/CASE-*/
assets/bgm/**/*.mp3
assets/bgm/**/*.wav

# 素材图片必须入库 —— 云端会话的出网策略拦截了所有图片站，
# git 是把图片送进渲染环境的唯一通路。
# 只排除我为验证排版生成的合成占位图。
topics/assets/_synthetic/
